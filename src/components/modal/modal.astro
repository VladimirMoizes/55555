---
import styles from './modal.module.css';
---

<div id="myModal" class={styles.modal} style="display: none;">
  <div class={styles.modalContent}>
    <button id="closeModalBtn" class={styles.closeBtn}>✕</button>
    <div>
      <slot />
    </div>
  </div>

<script src="./modal.ts"></script>

<!-- <div id="myModal" style="display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.5); backdrop-filter: blur(4px); z-index: 1000; align-items: center; justify-content: center;">
  <div style="background: #282828; border: 1px solid #fdbd6e; border-radius: 12px; padding: 45px; position: relative;">
    <button id="closeModalBtn" style="position: absolute; top: 16px; right: 16px; background: none; border: none; color: #fdbd6e; font-size: 24px; cursor: pointer;">✕</button>
    <div style="color: white;">
      <slot />
    </div>
  </div>
</div>

<script>
  const modal = document.getElementById('myModal');
  const closeBtn = document.getElementById('closeModalBtn');

  if (modal) {
    (window as any).openModal = () => {
      modal.style.display = 'flex';
    };
    (window as any).closeModal = () => {
      modal.style.display = 'none';
    };
    if (closeBtn) closeBtn.onclick = () => { modal.style.display = 'none'; };
    modal.onclick = (e) => { if (e.target === modal) modal.style.display = 'none'; };
  }
</script> -->
